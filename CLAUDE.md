# PHOTO IROHA サイト（事業者向け）

荒井秀治（PHOTO IROHA・長野）の**事業者向け（B2B）**サイト。
公開中：https://photoiroha.github.io/kenchiku.html
リポジトリ：https://github.com/PHOTOIROHA/PHOTOIROHA.github.io （公開）

デザインの検討経緯やコピーの決定履歴は `~/Desktop/hideji_work/ClaudeCode/写真（プラン／事業）/docs/` にある。
このリポジトリは「実際に公開されるもの」だけを持つ。

**同じ屋号で、奥様が主体のB2Cサイトが別に稼働している**（https://photoiroha.jimdofree.com/ ）。
将来つなげる方針。詳細は `docs/サイト戦略_2サイト体制とドメイン.md`。**まだリンクは張らないこと。**

## 触り方

```bash
python3 build.py           # 公開フォルダ（docs/）にHTMLを書き出す
python3 build.py --draft   # 表題欄に DRAFT を出した状態で書き出す（本人確認用）
python3 build.py --preview # preview/ に1枚完結のHTMLを書き出す（Artifact公開用）
```

必要なものは Python だけ。npm も外部ライブラリも使わない。
表示確認は `docs/` で `python3 -m http.server` を立ち上げる。

**公開する手順**（これで1〜2分後にサイトへ反映される）

```bash
cd ~/Sites/photoiroha && python3 build.py && git add -A && git commit -m "変更内容" && git push
```

## 構成

| 場所 | 中身 |
|---|---|
| `src/content/*.json` | **文章と写真の指定。ふだん編集するのはここ。** |
| `src/templates/base.html` | 全ページ共通の外枠（head・ヘッダー・フッター） |
| `src/templates/page_*.html` | ページごとの本体 |
| `src/styles/site.css` | 全ページ共通のCSS。色は `:root` の変数 |
| `photos/<ページ>/` | サイト用に縮小済みの写真。元データは置かない |
| `assets/` | ロゴ、ファビコン、OGP画像 |
| `docs/` | **ビルド結果。手で編集しない。** GitHub Pages がここを公開する |
| `tools/prepare_photos.py` | 元写真をサイト用サイズに書き出す |

`docs/` という名前は GitHub Pages の制約（公開できるのは `/` か `/docs` だけ）。
会話の中では、混同を避けるためこれを**「公開フォルダ」**と呼ぶこと。`docs/` と呼ぶのは文書置き場の方だけ。

## いまの状態

**できているもの**

- 建築ページ（`kenchiku.html`）。見出しは明朝、冒頭は大きな写真から
- お問い合わせボタン → `mailto:`（件名「建築写真の撮影について」を自動で入れる）
- OGP（メールやSNSにURLを貼ったときのサムネイルと説明文）
- ファビコン（ロゴのカメラとレンズ部分を切り出したもの）
- 404ページ。存在しないURLでも自作の画面が出る
- ライト／ダーク両対応。ロゴはダークで自動反転

**まだのもの**

- **トップページ**。`https://photoiroha.github.io/` は現在404。最優先
- **人物ページ**。コピーは2026年7月に確定済み（見出し「撮影が苦手な人ほど、満足していただけます。」、本文に「20年間、撮影技術に関するクレームは一度もありません」）だが、**人物写真の素材がない**
- **料理ページ**。コピーが短いまま（「一皿の完成度を、そのまま写真に。」のみ）。詳細版が必要。写真は `#ポートフォリオ用/f_*` にある
- sitemap.xml / robots.txt、アクセス解析
- 独自ドメイン `photoiroha.jp` への移行（2026-09-11時点で取得可能なことを確認済み。未取得）

## 決まっていること

- 見出しは明朝（Shippori Mincho）、本文はゴシック（BIZ UDGothic）、ラベルは M PLUS 1 Code
- 建築ページの冒頭は**大きな写真から始める**（2026-09-10 決定）
- 背景は白に寄せる。写真の色を邪魔しない
- 経歴は囲みではなく、本文の下に小さな一行で置く（囲み4つは「大袈裟」と本人判断）
- ライト／ダーク両対応。色は必ず `:root` の変数を通す

## 触ってはいけないもの

- **建築ページの本文**。本人が「AI臭さを減らしたい」として書き直す予定。こちらから書き換えない
- **家族写真**。施主のご家族（お子さん2人を含む）が写っており、web掲載の承諾が未確認。
  `~/Desktop/hideji_work/ClaudeCode/_一時/建築ページ_保留写真/` に封印してある。
  **本人が明示的に許可するまで、リポジトリに戻さない。** 別の家族写真を用意する予定あり。
  AIで人物を差し替える案は、本人と検討したうえで見送っている

## 写真を差し替えるとき

```bash
python3 tools/prepare_photos.py kenchiku 01-ldk /path/to/元写真.jpg
python3 tools/prepare_photos.py kenchiku hero  /path/to/元写真.jpg --hero
```

そのあと `src/content/kenchiku.json` の `alt`（写真の説明文）も直す。読み上げとSEOで使われる。

写真の元データは `~/Desktop/hideji_work/#作業用/HP/建築/素材/` にある。
`#ポートフォリオ用/a_0001〜a_0011` が建築、`f_` で始まるものは料理。

**確認を怠らないこと：表札・住所プレート・車のナンバーが写っていないか。**
建売物件はすでに買主が住んでいるので、特定につながる情報は出さない。所在地・分譲地名・区画番号も書かない。
（`20250630_Photo_Data_外観/R62_4909.jpg` は車のナンバーが読めるため不使用）

## ページを増やすとき

1. `build.py` 冒頭の `SITE` で、そのページの `ready` を `True` にする
2. `src/content/<ページ名>.json` を作る
3. `src/templates/page_<ページ名>.html` を作る（建築ページを写すのが早い）

`ready` が `False` のページは、ナビに「準備中」と出るだけでリンクにならない。

## 本人について

プログラマーではない。コマンドは実行できるが、仕組みの説明は必要。説明は噛み砕きすぎなくてよい。
写真の選定は本人の領分なので、こちらが選んだものは必ず「仮」と伝えること。
副作用のある操作は、内容と件数を先に提示して承認を得る。
事実・一般論・推測・提案は全角パイプ（｜事実｜ など）で明示する。
